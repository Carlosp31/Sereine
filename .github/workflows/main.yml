name: deploy
on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: [self-hosted, carlos]

    steps:
      - name: Mostrar info básica
        run: |
          whoami
          pwd
          ls -la
